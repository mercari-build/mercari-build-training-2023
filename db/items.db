create table items(
        id integer primary key,
        name text,
        category integer,
        image_name text
);

create table category(
        id integer primary key,
        name text
);
